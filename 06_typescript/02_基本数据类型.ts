let isDone: Boolean = true

let createByNewBoolean: Boolean = new Boolean(1)

function say(): void {
  alert(1)
}

let un: void = undefined

let u: undefined = undefined
let u2: undefined
let num: number = u2
let n: null = null

let myFavoriteNum: any = 7

let anything: any = 'hello'
console.log(anything.name) // 任意值可以访问任意属性
