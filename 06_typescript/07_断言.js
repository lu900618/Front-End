function getLength(x) {
    if (x.length) {
        return x.length;
    }
    else {
        return x.toString().length;
    }
}
function getLength2(x) {
    if (x.length) {
        return x.length;
    }
    else {
        return x.toString().length;
    }
}
